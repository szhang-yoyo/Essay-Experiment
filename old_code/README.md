# Old Code

This directory retains earlier or superseded experiment and analysis scripts so that the development history of the project remains visible.

These files are not used to generate the final dissertation dataset. They are preserved rather than deleted, following the project provenance policy.

The active scripts used for the final dissertation experiments remain in `scripts/`.
